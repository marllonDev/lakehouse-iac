[project]
name = "lakehouse-iac"
version = "0.1.0"
description = "Terraform + dbt + Databricks Free Edition study project"
requires-python = ">=3.11"
dependencies = [
    "dbt-databricks>=1.12.4",
]

[tool.uv]
package = false
